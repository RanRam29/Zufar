services:
  - type: web
    name: zufar-backend
    env: python
    buildCommand: pip install -r backend/requirements.txt
    startCommand: ./scripts/start.sh
    autoDeploy: true
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
      - key: LOG_LEVEL
        value: INFO
      - key: ALEMBIC_UPGRADE_TARGET
        value: head
    healthCheckPath: /healthz

  - type: web
    name: zufar-frontend
    env: static
    buildCommand: npm ci && npm run build
    staticPublishPath: dist
    autoDeploy: true
    envVars:
      - key: NODE_VERSION
        value: 20
    routes:
      - type: rewrite
        source: "/*"
        destination: "/index.html"
